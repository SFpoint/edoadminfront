import { repository, baseURL } from "@/api/base"

export default {
  // async getAdmConditions() {
  //   const { data } = await repository.get(baseURL + '/enum/forAdmCondition')
  //   return data
  // },
}